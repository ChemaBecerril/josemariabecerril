# Fieldwork media

Put the images and videos for the Fieldwork page in this folder.
Then list them, in order, with their texts, in `src/data/fieldwork.js`.

The `placeholder-*.jpg` files can be deleted once they are no longer
referenced in `src/data/fieldwork.js`.
